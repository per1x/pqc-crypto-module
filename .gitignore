# Build outputs
build/
build-asan/
build-tsan/
compile_commands.json

# Python
__pycache__/
*.pyc
.venv-rtl/
.venv-p11/

# Simulation and synthesis outputs
# Makefile.trng 给每个顶层单开一个构建目录（sim_build_trng_axi 等），所以带通配。
hardware/tb/cocotb/sim_build*/
# 并行跑多个用例时会用 SIM_BUILD=sb_xxx 各开一个构建目录（互不覆盖），一并忽略
hardware/tb/cocotb/sb_*/
# tools/mldsa_grid.sh 每格开一个 g_<仿真器>_<参数集>_<模块>/ 构建目录，
# 跑完就删 —— 但并发提交会正好逮到在飞的那一个（本仓库已经被逮到过一次，
# 12k 行的 sim.vvp 混进了提交）。所以这里也要挡。
hardware/tb/cocotb/g_*/
hardware/tb/cocotb/results.xml
# 并行跑九格矩阵时每格各一份结果与日志（COCOTB_RESULTS_FILE=results_<格>.xml）
hardware/tb/cocotb/results_*.xml
hardware/tb/cocotb/log_*.txt
hardware/syn/rpt/

# Test vectors are reproducible derivatives (tools/fetch_vectors.sh pins the ACVP
# commit): 13 MB of raw JSON plus 4.6 MB of flattened output. Not tracked.
vectors/

# Fuzzing corpus
.fuzz-corpus/

.DS_Store

# TEE build outputs
tee/ta/out/
tee/tests/run_tests
tee/tests/interop_test
build-demo/

# 远程口的 mTLS 凭据。**私钥绝不进仓库** —— tools/mkpki.sh 现生成。
pki/

# service/ 是 Makefile 构建的（不进 CMake），产物落在源码目录里
service/*.o
service/*.a
service/pqchsm_fpgad
service/sdf_demo
service/sslprobe*
service/ctorprobe
service/h_static
service/h_dyn
